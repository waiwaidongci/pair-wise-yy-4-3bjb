# 水下考古潜水记录

直接打开`index.html`使用。点击平面图添加标记，支持筛选、编辑、删除、时间线和导出JSON。

## 出水晾护交接台

页面下方为出水晾护交接台，每条记录登记出水时刻、材质、含水率和当前容器：

- 木质件离水30分钟未入保湿箱即进入风险清单；
- 陶片含水率高于18%须先阴干，不得入恒湿柜；
- 保管员接手须核对当前容器与上一步时间；换容器保留旧段并自动重算风险；
- 待办按剩余时限排序，每15秒刷新倒计时。

代码分三层独立承载：判定层（纯规则`judgeCare`）、档案层（`CareStore`存取与容器段管理）、界面层（渲染与交互）。

